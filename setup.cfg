[metadata]
name = financial_analytics_bankruptcy_prediction_suite
version = 0.0.0

[options]
package_dir =
    = src
packages = find:
install_requires =
    # keep dependencies in requirements.txt; add any runtime deps here if you prefer

[options.packages.find]
where = src
